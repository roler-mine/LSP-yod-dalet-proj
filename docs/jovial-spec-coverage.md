# JOVIAL Spec Coverage

This tracker is a conservative snapshot of current lexer, parser, AST, semantic,
navigation, diagnostic, and test coverage. It is intended for planning and should
be updated whenever a feature moves from generic parsing or editor-only support
to spec-aware behavior.

Status values used in the table: `not started`, `partial`, `mostly complete`,
`complete`.

Last refreshed: 2026-05-17 as part of the parser recovery, Chapter 12
built-in, `REP`, bang-directive, `!LINKAGE`, and assembly-label support pass. Coverage
states remain conservative and reflect implemented parser/semantic/navigation
behavior plus tests, not full target-specific compiler semantics.

| Feature | Lexer | Parser | AST | Semantics | Navigation | Diagnostics | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ITEM declarations | complete | mostly complete | mostly complete | mostly complete | mostly complete | mostly complete | mostly complete | `ITEM` is tokenized and parsed into `DVar` with `DataItem`; common hover, definition, references, completion, inlay, and type mismatch paths exist. Some declaration attributes are still parsed generically or ignored. |
| CONSTANT ITEM declarations | complete | mostly complete | mostly complete | partial | mostly complete | partial | partial | `CONSTANT ITEM` parses into `DConst` and has a storage/allocation warning, but constant-specific semantic rules remain shallow. |
| TABLE declarations | complete | partial | mostly complete | partial | mostly complete | partial | partial | Several table forms, dimensions including open `(*)` formal dimensions, presets, and record fields parse into `TArray`/`TRecord`. Field ownership keeps same-name fields in different table owners separate for owner-qualified navigation/references/rename, simple dimension/preset diagnostics exist, and a conservative logical layout model can report simple table entry/field sizes. Full target-accurate layout remains future work. |
| CONSTANT TABLE declarations | partial | partial | partial | partial | mostly complete | partial | partial | Common `CONSTANT TABLE NAME(...)`, typed, and record-body forms parse as readonly `DConst` table data with `JovialConstantTable` metadata. Hover, inlay, document/workspace symbols, and plain assignment diagnostics are covered; full preset/layout and element-assignment semantics remain conservative. |
| BLOCK declarations | complete | partial | partial | partial | mostly complete | partial | partial | `BLOCK` parses into a `DVar` with `DataBlock` and record-like fields. Block fields participate in owner-scoped metadata/navigation foundations, but full block layout and representation semantics are not implemented. |
| TYPE declarations | complete | mostly complete | mostly complete | partial | mostly complete | partial | partial | Builtin and user type declarations are parsed and surfaced in navigation; the semantic type system is still coarse for many JOVIAL rules. |
| PROC definitions | complete | mostly complete | mostly complete | mostly complete | mostly complete | mostly complete | mostly complete | `PROC` declarations with bodies, parameters, return types, `REC`, `RENT`, and procedure-local `!LINKAGE` clauses have broad support; legacy `ILINKAGE` spelling is accepted as an alias. Advanced procedure attributes are not complete. |
| FUNCTION definitions | partial | partial | partial | partial | mostly complete | partial | partial | Return-typed `PROC` declarations are treated as functions in metadata and navigation; there is no separate `FUNCTION` keyword path. |
| DEF declarations | complete | mostly complete | mostly complete | partial | mostly complete | partial | partial | `DEF` modifiers and `DEF BEGIN ... END` groups are represented, and exported symbols participate in navigation/import logic. Cross-module precision is still evolving. |
| REF declarations | complete | mostly complete | mostly complete | partial | mostly complete | partial | partial | `REF` modifiers and groups are represented, and imported items/procs suppress common unresolved diagnostics. Full external contract checking is not implemented. |
| COMPOOL modules | complete | mostly complete | mostly complete | mostly complete | mostly complete | mostly complete | mostly complete | COMPOOL definitions are scanned, indexed, persisted, and used for imports, diagnostics, navigation, code actions, and CodeLens counts. |
| !COMPOOL directives | complete | mostly complete | mostly complete | mostly complete | mostly complete | mostly complete | mostly complete | `!COMPOOL` imports are parsed/scanned and drive import visibility, hints, diagnostics, quick fixes, and indexing. Legacy `ICOMPOOL`/`!ICOMPOOL` spellings are canonicalized to `!COMPOOL`. |
| !COPY directives | partial | partial | partial | partial | partial | partial | partial | `!COPY` has an explicit include-target/source-map model, resolved dependency edges, reverse include users, unresolved/cyclic diagnostics for detectable cases, and debug-report visibility. Legacy `ICOPY`/`!ICOPY` spellings are accepted as aliases. Generic bang compiler directives are acknowledged with loose payload parsing so they do not cascade parser errors, including in statement blocks, but full directive semantics remain provisional. |
| DEFINE macros | complete | mostly complete | mostly complete | partial | mostly complete | mostly complete | mostly complete | DEFINE lexing, parsing, expansion, source mapping, macro graph, hover, goto definition, references, and conservative rename of macro declarations/call sites exist. Rename rejects generated expansion symbols by default; full macro substitution semantics remain future work. |
| DEFINE parameters | complete | mostly complete | mostly complete | partial | mostly complete | mostly complete | mostly complete | Formal parameters, actuals, macro graph actual display, and arity diagnostics exist. Full list option and substitution semantics remain conservative. |
| Generated names | partial | partial | partial | partial | partial | partial | partial | Apostrophe-containing and quoted identifiers are accepted in common paths, and rename rejects unsafe generated macro expansion symbols by default. Generated-name rules are still not modeled as a distinct semantic feature. |
| STATUS values | partial | mostly complete | mostly complete | partial | mostly complete | partial | mostly complete | `STATUS (V(...))` lists are represented in AST/type metadata, status constants are indexed as `JovialStatusConstant`, and clear duplicate/member/ambiguous cases have diagnostics. Explicit representation rules remain future work. |
| Pointer types | partial | mostly complete | mostly complete | mostly complete | mostly complete | partial | mostly complete | `P` and `P TYPE` forms map to pointer types, with hover/inlay/type display and coarse compatibility support. Pointer conversion and target rules need more diagnostics. |
| Pointer-qualified references | complete | mostly complete | mostly complete | partial | partial | partial | partial | `@` dereference and field-at-pointer syntax parse to `EDeref`/`EAt`; semantic checking exists for typed pointer dereference and fields but is conservative. |
| Chapter 12 built-in functions | partial | mostly complete | partial | partial | partial | partial | partial | Builtins are completion/import-safe names and parse as ordinary calls. Semantic result typing and argument diagnostics exist for `LOC`, `NEXT`, `BIT`, `BYTE`, `SHIFTL`, `SHIFTR`, `ABS`, `SGN`, `BITSIZE`, `BYTESIZE`, `WORDSIZE`, `LBOUND`, `UBOUND`, `NWDSEN`, `FIRST`, and `LAST`; `BIT`/`BYTE` pseudo-variable assignments parse and use the selected substring length for assignment checking. `!LINKAGE BIF` on matching `PROC` declarations is preserved, so dialects that declare built-ins through BIF linkage do not lose that contract. Type-name arguments are recognized for size, `NWDSEN`, `FIRST`, and `LAST` paths. Target-accurate runtime values, compile-time evaluation of size/bounds functions, and detailed layout/status-bound results remain future work. |
| Conversion operators | complete | mostly complete | mostly complete | partial | partial | partial | partial | `(* type *) expr` lexes as conversion brackets and parses to `EConvert`; `REP(expr)` is recognized as the J73 representation conversion, returns a bit representation, may be used as a pseudo-variable lvalue, and warns on obvious non-reference arguments. Other conversion legality and narrowing/widening diagnostics are not complete. |
| Integer formulas | complete | mostly complete | mostly complete | partial | partial | partial | partial | Integer literals and arithmetic expressions parse, with coarse integer compatibility. Width, signedness, overflow, and formula-specific rules are not complete. |
| Float formulas | complete | mostly complete | mostly complete | partial | partial | partial | partial | Float literals and arithmetic parse, but precision and conversion rules are coarse. |
| Fixed formulas | partial | mostly complete | mostly complete | partial | partial | partial | partial | Manual `A scale,fraction` forms such as `A 2,13` parse, display, and flow through metadata/type checking as fixed types. Detailed target-specific fixed arithmetic remains conservative and currently shares coarse numeric behavior with floats except for explicit fixed/integer mixing warnings. |
| Bit formulas | complete | mostly complete | mostly complete | partial | partial | partial | partial | Based bit literals and boolean/bit operators are tokenized/parsed; bit length and operator result rules are only coarse. |
| Character formulas | partial | partial | partial | partial | partial | partial | partial | Quoted text is tokenized as strings and typed by length in semantic checks; full character/string formula rules are not modeled. |
| Status formulas | partial | partial | partial | partial | mostly complete | partial | partial | `V(...)` remains a compatible constructor expression, with membership diagnostics when an expected STATUS context is known and ambiguity warnings when no context disambiguates duplicate value names. Full representation/conversion semantics remain conservative. |
| Table formulas | partial | partial | partial | partial | partial | partial | partial | Indexing, fields, and presets parse and are walked by navigation/semantics, but table formula typing is intentionally shallow. |
| CASE statements | complete | partial | partial | partial | partial | not started | partial | `CASE` parses enough to preserve/check option bodies, but the scrutinee and case indexes are not represented as a dedicated AST statement. |
| FOR loops | complete | mostly complete | mostly complete | partial | partial | partial | partial | `FOR` parses to `SFor` with loop-control typing and loop-depth checks, but complete loop semantics are not implemented. |
| WHILE loops | complete | mostly complete | mostly complete | partial | partial | partial | partial | `WHILE` parses to `SWhile` and participates in loop-depth diagnostics; condition typing remains coarse. |
| EXIT/GOTO/RETURN/ABORT/STOP | complete | mostly complete | partial | mostly complete | partial | mostly complete | partial | Control statements parse and have diagnostics for loop/procedure/label misuse. `EXIT`, `ABORT`, and `STOP` currently reuse call-statement representation. |
| Table presets | complete | partial | partial | partial | partial | partial | partial | Presets parse into `EPreset` for simple forms; detailed preset validation and positioners are not complete. |
| Table dimensions | complete | mostly complete | mostly complete | partial | partial | partial | mostly complete | Dimensions, ranges, open `*` formal dimensions, and entry-count hints exist for common cases. Bounds, status dimensions, and full validation are incomplete. |
| Specified tables | partial | partial | partial | partial | partial | partial | partial | Common `TABLE ... W entry-size`/`V` record forms and field `POS(startbit,startword)` clauses now have AST representation, hover facts, conservative logical layout facts, and shallow diagnostics for missing/invalid/duplicate/out-of-entry/overlap positions. Full packing, overlay layout interaction, and target-specific representation rules remain future work. |
| Table packing | not started | not started | partial | partial | partial | partial | partial | `Jovial_layout` now models V1 logical table entry sizes, simple field offsets, specified-table POS ranges, hover/debug facts, and exact overlap/exceeds-entry diagnostics. It deliberately does not claim target-accurate packing or code-generation layout. |
| OVERLAY declarations | partial | partial | partial | partial | partial | partial | partial | Common `OVERLAY NAME (...)`, optional `POS(address)`, target lists, nested groups, and `SPACER(expr)`/`SPACE(expr)` forms now parse into AST with overlay metadata, hover/document symbols, and shallow diagnostics for unknown/duplicate targets and simple invalid POS/spacer expressions. Full storage layout sharing remains future work. |
| Implementation parameters | partial | partial | partial | partial | partial | partial | partial | Procedure formals and colon-separated out parameters parse, with limited type inference from local declarations. A configurable implementation profile now carries dialect, word/byte sizes, default float/fixed precision, max-size hints, and system subroutine names from environment or VS Code settings; compile-time formulas, type display, layout facts, and debug reports consume the known values while unknown target rules stay conservative. |
| Inline declarations | partial | partial | partial | partial | partial | partial | partial | `INLINE PROC` and procedure-header `INLINE` are preserved on procedure AST/metadata, surfaced in hover/signature detail, highlighted as a keyword, and obvious non-procedure targets get a parser diagnostic. Full procedure-target legality remains conservative. |
| Readonly declarations | partial | partial | partial | partial | partial | partial | partial | `READONLY ITEM`/`TABLE` and trailing item `READONLY` are preserved on data AST/metadata, shown in hover, carried through cache metadata, highlighted as a keyword, and simple assignments to readonly data are diagnosed. Complex aliasing and target-specific readonly variants remain future work. |
| Machine-specific subroutines and assembly linkage | partial | partial | partial | partial | partial | partial | partial | Configured system subroutine names are modeled as `ExternalSystem`, suppress unresolved-procedure diagnostics, and surface system/built-in hover. Procedure-local `!LINKAGE symbol;` clauses parse and are preserved on PROC AST nodes. Assembly support is label-based: discovered `.asm` support files are not parsed as Jovial, but labels matching a `PROC` name can suppress unresolved-procedure diagnostics and serve as implementation targets. ASM `!COMPOOL` imports scope labels to Jovial files that import the same COMPOOL; ASM files without a COMPOOL import remain globally visible for compatibility. Calling-convention checking, object/linker metadata, and target-specific assembly syntax remain future work. |
